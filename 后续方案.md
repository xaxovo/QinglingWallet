# 后续方案：查账 / 自动记账（前台补全）/ 桌面小组件（可自定义）

> 状态：待批准。批准后按此实现，完成后同步更新 README / CHANGELOG。

## 1. 查账更顺手（A 部分）
- **搜索 + 筛选**：明细页顶部加搜索框（按备注），筛选（分类 / 收支 / 金额区间）。本地 SQL LIKE + 条件过滤。
- **历史月份切换**：统计页和明细页顶部加月份选择器（上月/下月箭头），数据按 `ts` 月份过滤；统计聚合查询按所选月份参数化。
- **日历视图**：明细页提供「日历」入口，月历格子显示每天是否记账，点某天看当天记录（复用现有明细卡片）。

## 2. 自动记账（重复规则 + 回到前台自动补全）
**数据**：新增 `recurring_rules` 表（id, 名称, 金额, 分类, 类型, 频率[weekly/monthly/yearly], 周期日期, 备注, 启用）。
**触发**：App **回到前台**（`WidgetsBindingObserver` 的 `resumed`）时自动检查所有启用规则：
- 判断当天是否命中（周几 / 每月几号 / 每年几月几号），命中且该周期内**尚未生成**则自动补记一笔；已生成则跳过。
- 补记后在首页给一条提示「已自动补记：xxx」。
**说明**：不依赖真后台，靠 App 前台触发，可靠、省电、无需后台权限。真正"到点必记"做不到（安卓限制），但每次打开 App 都会补全，不会漏。

## 3. 桌面小组件（花体 Qingling + 软件内自定义）
- 用 `home_widget` 包注册 Android AppWidget，卡面右上角放花体 **Qingling**（`Monsieur La Doulaise` 签名字体）。
- **点击小组件**：Intent 直接打开 App 的「记一笔」弹层（快速记账）。**不显示余额**。
- **软件内自定义接口**（我的页 →「小组件设置」），可调：
  - 花体**字体颜色**
  - **背景颜色**
  - **是否显示花体**（开关，关闭则只显示背景）
  - **更换背景图片**（从相册选图）
  - 保存后调用 `home_widget.updateWidget` 刷新小组件。

## 待拍板 / 说明
- 小组件默认样式按粉白主题（背景白/浅粉，花体深粉），用户可改，属接口。
- 小组件需打包字体 + 少量原生代码，我会一并接入并更新 README/CHANGELOG。
