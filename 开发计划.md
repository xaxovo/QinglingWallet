# 清零记账（QinglingWallet）开发计划

> 文档版本：v0.1（规划稿，待用户确认）
> 编写日期：2026-08-31
> 项目名：qinglingwallet　软件名：清零记账　平台：Android 手机端

---

## 1. 项目概述

- **定位**：一款**简洁易用、高度个人定制化**的个人记账 App。
- **核心目标**：打开即记、一步到位；记录无负担，视觉与交互达到 Apple / Google 级设计审美，操作拥有丝滑动画与强反馈。
- **管理后台/个人风格**：延续「清零」IP，内置个人化可爱彩蛋（萌图 + 玩味文案）。

## 2. 技术选型（待确认）

**推荐框架：Flutter**

理由：
- 原生级丝滑动效，同时具备 **Material 3（Google）** 与 **Cupertino（Apple）** 双设计语言，正好契合「Apple/Google 审美」要求。
- 跨平台：一套代码可扩展 iOS。
- 动画生态强：Implicit / Explicit Animation、Hero 共享元素、Rive / Lottie 微交互动画、`flutter_animate`、`HapticFeedback` 强反馈。
- 与本地已有 Flutter 项目（APRSLocus）技术栈一致，可复用经验。

配套技术：
| 类别 | 方案 | 说明 |
| --- | --- | --- |
| 状态管理 | Riverpod | 简洁、可测 |
| 路由 | go_router | 支持页面转场动画 |
| 本地数据库 | Isar / Drift(SQLite) | 离线优先、性能好 |
| 图表 | fl_chart | 统计饼/柱/趋势 |
| 本地化 | intl + arb | 中英双语 |
| 主题 | Material 3 + 动态取色 | 深浅色、自定义强调色 |

> ⚠️ **关键约束**：本机暂无 Flutter SDK / Android SDK / JDK / Gradle，**无法在本机编译运行 Android 包**。构建与运行方式需与用户确认（见第 8 节）。

## 3. 核心功能（简洁易用，MVP 优先）

1. **快速记账**：首页一键记一笔（金额 + 分类 + 备注），支持常用分类快捷入口。
2. **分类管理**：可自定义分类（名称 / 图标 / 颜色），增删改。
3. **明细列表**：按日期分组、搜索、筛选（收入/支出/分类）。
4. **统计**：月度收支概览、分类占比、趋势曲线。
5. **预算**：月度预算设置、超支提醒。
6. **数据**：本地存储，CSV 导出/导入，本地备份恢复。
7. **主题个性化**：深浅色、强调色、圆角/密度、App 图标。

## 4. 高度个人定制化

- 自定义分类体系、图标与颜色。
- 外观深度定制：主题、强调色、壁纸、圆角、列表密度。
- 手势与反馈强度可调（震动强度、动画开关）。
- 个人化彩蛋：开机/保存/节日触发萌图与玩味文案。
- 数据导出格式与项目自定义。

## 5. 设计语言（Apple / Google 审美）

- **Material 3 (Material You)** 动态色彩 + **Cupertino** iOS 质感，双风格统一。
- 大圆角、柔和阴影、毛玻璃（glassmorphism）点缀。
- Spring 弹性缓动曲线（`easeOutBack` / 弹簧 `bounce`）。
- 统一动效规范：页面转场、列表交错、按压缩放反馈，全app一致。

## 6. 动画与交互反馈（核心卖点）

- **页面转场**：Hero / 共享元素 / 滑动推进。
- **列表**：交错进入动画（staggered）。
- **按压**：缩放 + 触感反馈（scale on tap + ink）。
- **强反馈**：`HapticFeedback`（轻/中/重震感），随操作点按、滑动、删除触发。
- **保存成功**：丝滑确认动画 + 粒子/表情迸发彩蛋。
- **金额滚动**：数字滚动动画。
- **卡片/浮层**：弹性出现与收起。

## 7. 项目结构（规划）

```
qinglingwallet/
├── lib/
│   ├── main.dart
│   ├── app.dart                 # 应用入口
│   ├── core/                    # 主题、路由、工具
│   │   ├── theme/               # 设计 tokens、深浅色
│   │   ├── router/              # go_router 与转场
│   │   └── utils/               # 动画、震动、格式化
│   ├── data/                    # 数据库、模型、仓库
│   ├── features/
│   │   ├── home/                # 首页 + 快速记账
│   │   ├── transactions/        # 明细/分类/搜索
│   │   ├── stats/               # 统计图表
│   │   ├── budget/              # 预算
│   │   └── settings/            # 个性化定制
│   └── widgets/                 # 共享组件（SoftCard 等）
├── assets/                      # 图标、萌图、彩蛋
├── pubspec.yaml
├── README.md                    # 功能与实现说明
└── CHANGELOG.md                 # 版本变更日志
```

## 8. 疑难点 & 待确认（重点）

1. **框架选择**：Flutter（推荐）还是原生 Kotlin/Jetpack Compose？
2. **构建与运行**：本机无 Android/Flutter 工具链，如何执行？
   - 本机安装全套工具链；或
   - 只写代码，交回用户环境构建；或
   - 配置 CI 构建。
3. **数据方案**：纯本地（Isar/Drift），是否需要云同步/登录？
4. **彩蛋风格**：确认个人化萌图与玩味文案的方向。
5. **MVP 范围**：首版功能是否按第 3 节，或需增删。

## 9. 开发阶段 / 里程碑

- **M0 规划确认**：确认技术选型与功能范围（本文档）。
- **M1 项目搭建**：+ 主题 + 路由 + 共享组件。
- **M2 数据层**：数据库 + 记账核心逻辑。
- **M3 功能**：明细 / 分类 / 统计。
- **M4 增强**：预算 + 导入导出 + 备份。
- **M5 打磨**：动画 + 强反馈 + 彩蛋。
- **M6 测试 / 发布**：自测 + 交付。

## 10. 版本管理与日志规范

- 每个变更记入 `CHANGELOG.md`。
- `README.md` 注释全部功能与实现方式。
- 遵循用户确认后再执行开发，不擅自提交推送。

---
*（规划稿至此，请确认后进入开发）*
